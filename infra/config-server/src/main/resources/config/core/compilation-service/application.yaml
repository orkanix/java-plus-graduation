server:
  port: 0

spring:
  datasource:
    url: jdbc:postgresql://localhost:9999/compilation
    username: compilation
    password: compilation
  jpa:
    properties:
      hibernate:
        default_schema: public
        format_sql: false
    hibernate:
      ddl-auto: create-drop
    database-platform: org.hibernate.dialect.PostgreSQLDialect